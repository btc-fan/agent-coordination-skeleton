# Accessibility

When derived projects expose dashboards or control surfaces:
- keyboard-first interaction where possible
- high-contrast status states
- clear labels for active/blocked/stopped/done lanes
- avoid relying on color alone for workflow status
