# UI / UX Rules

This project is infrastructure-first, but future derived projects should preserve:
- low-noise operator interfaces
- clear separation of control vs ops channels
- deterministic, short command grammar
- visible verification state
- no ambiguous “done” states without evidence
